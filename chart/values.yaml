namespace: code-server

nameOverride: ""

commonLabels:
  app: code-server

auth:
  usePassword: true
  existingSecret: ""

deploy:
  image:
    registry: docker.io
    repository: massipssa/coder-server
    tag: 0.0.1
    pullPolicy: IfNotPresent
  replicaCount: 1
  containerPorts:
    http: 8080
  resources:
    limits: {}
    requests: {}

service:
  ports:
    http: 8080
  port: 80
  targetPort: 8080


ingress:
  enabled: true
  ingressClassName: ""
  hostname: coder.local
  pathType: Prefix
  path: /
